# react-redux-public-blog
Blog platform for everyone to post and make them public, web application using React-n-Redux

## Running steps

After cloning the repository, run these command lines:

    $ npm install
    $ npm start